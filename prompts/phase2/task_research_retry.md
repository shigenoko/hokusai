あなたの前回の出力は仕様違反でした。以下のエラーが発生しました:

{validation_error}

前回の出力（仕様違反）:
---
{previous_output}
---

この出力は使用できません。以下の元タスク情報と調査手順に基づき、
調査レポートを全文再生成してください。

## 元タスク情報

タスクURL: {task_url}

## 調査手順

1. NotionタスクURL を mcp__notion__notion-fetch で読み取り、タスク名・要件・関連エンティティを把握する
2. コードベースを調査する:
   - エンティティの型定義・フィールド仕様
   - 既存のAPI・サービス・コントローラー実装
   - 類似実装パターン（同一エンティティを優先）
   - 影響範囲の特定
3. 調査結果をレポートとして出力する

## 絶対厳守ルール

1. 先頭行は必ず「## 事前調査レポート」とすること
2. 前置き文を絶対に含めないこと（「調査完了しました」「了解しました」「レポートを出力します」等）
3. 要約を出力しないこと
4. メタ説明を出力しないこと（「手動でNotionに貼り付けてください」等）
5. 全文Markdown本文のみを出力すること
6. 「## 事前調査レポート」以外のテキストを先頭に一切出力しないこと

## 必須セクション

- 「### タスク概要」 — タスク名、タスク種別
- 「### 1. 現状」 — 機能ごとの実装状態を表で整理
- 「### 2. 関連エンティティ仕様」 — 型、制約、定義場所を表で整理
- 「### 3. 必要な変更」 — レイヤー、ファイル、変更内容を表で整理
- 「### 4. 参考実装」 — 参考にすべき既存ファイル
- 「### 5. 確認事項」 — 実装前に確認が必要な事項

前回の調査内容をそのまま使い、フォーマットだけを修正して全文を再出力してください。
前置き文の削除ではなく、「## 事前調査レポート」から始まる全文を出力してください。